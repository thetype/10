/* global doT, wx */

const ENV = ENV || 'production';

// Global namespace for the New Money Selected app
const TIB = TIB || {};

/*
TIB.API_DOMAIN =
	ENV === 'production'
	? 'http://thetype.com'
	: 'http://test.thetype.com';
*/
